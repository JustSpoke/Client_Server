//
//  LoginView.swift
//  lesson_1
//
//  Created by Vladimir Kobelev on 29.10.2021.
//

import UIKit

class LoadingDot: UIView {
/*
   private var stackViews: UIStackView!
    private var images = UIImage(systemName: "circle.fill")!
    var color: UIColor = .black
    var spacing: CGFloat = 10
    
    private func setupView() {
        var dot = [UIImageView]()
        
        for _ in 0..<3 {
            let imageView = UIImageView()
            imageView.image = images
            imageView.tintColor = color
            
            dot.append(imageView)
        }
        
        stackViews = UIStackView(arrangedSubviews: dot)
        self.addSubview(stackViews)
        
        stackViews.spacing = spacing
        stackViews.axis = .horizontal
        stackViews.alignment = .center
        stackViews.distribution = .fillEqually
        stackViews.frame = bounds
        stackViews.backgroundColor = .clear
        
        stackViews.isLayoutMarginsRelativeArrangement = true
        stackViews.layoutMargins = UIEdgeInsets(top: 1, left: 35, bottom: 1, right: 35)
        
    }
    
    func animateDots() {
        UIView.animate(withDuration: 0.5,
                       delay: 0.5,
                       options: [.repeat, .autoreverse],
                       animations: {
            self.stackViews.subviews[0].alpha = 0.3
        })
        UIView.animate(withDuration: 0.5,
                       delay: 0.8,
                       options: [.repeat, .autoreverse],
                       animations: {
            self.stackViews.subviews[1].alpha = 0.3
        })
        UIView.animate(withDuration: 0.5,
                       delay: 1.1,
                       options: [.repeat, .autoreverse],
                       animations: {
            self.stackViews.subviews[2].alpha = 0.3
        })
    }
    override init(frame: CGRect) {
        super.init(frame: frame)
        self.setupView()
    }
    required init?(coder aCoder: NSCoder) {
        super .init(coder: aCoder)
        self.setupView()
    }
*/
}
 
